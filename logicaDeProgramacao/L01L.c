#include <stdio.h>

int main(void) {

int A, B, C, somaDosQuadrados;

printf("Informe respectivamente A, B e C: ");

scanf("%d %d %d",&A, &B, &C);

somaDosQuadrados = (A*A)+(B*B)+(C*C);

printf("A soma dos quadrados dos valores são: %d",somaDosQuadrados);

  return 0;
}
